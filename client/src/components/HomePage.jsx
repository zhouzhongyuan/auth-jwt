import React from 'react';
import { Card, CardTitle } from 'material-ui/Card';

const HomePage = () => (
    <Card className="container">
        <CardTitle title="React 应用" subtitle="这是主页"/>
    </Card>
)
export default HomePage;